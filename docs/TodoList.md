# 240611

- [x] 将耗时组件改为并发执行

# 240607

- [x] 更新 `机器学习回归训练` 与 `机器学习回归评估` 功能代码
- [x] 检查 `模型载入` 与 `机器学习模型应用` 的协作情况

# 240531

- [x] 尽可能为所有组件适配与 `测井数据加载` 的连接

# 240518

- [x] 检查 `分层数据加载` 到 `分层数据处理` 的连接
- [x] 适配 `基于相关系数的层次聚类算法` 与 `测井数据加载` 的连接
